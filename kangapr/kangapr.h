#pragma once

const int cMinSeqLen = 10;		// sequences accepted must be of at least this length
const int cMaxSeqLen = 10000;	// and less than or equal to this length

const int cMaxEndTrim = 1000;	// allow end trimming of at most this number of bases

const int cMaxAdaptSeqLen = 40; // allow adaptor sequences to be at most this length
const int cMaxAdaptSeqs = 20;   // allow at most this number of adaptor sequences to be specified

const int cMaxFileNamePrefix = 40; // allow file name prefixes to be at most this length

